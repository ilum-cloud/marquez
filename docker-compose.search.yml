version: "3.7"
services:
  api:
    depends_on:
      - db
      - opensearch
    environment:
      - SEARCH_PASSWORD=CHANGEMEPLEASE1@#a
    entrypoint: ["./wait-for-it.sh", "db:${POSTGRES_PORT}", "--timeout=60", "--", "./wait-for-it.sh", "opensearch:${SEARCH_PORT}", "--timeout=60", "--", "./entrypoint.sh"]

  opensearch:
    image: opensearchproject/opensearch:2.19.4
    container_name: marquez-search
    hostname: opensearch
    ulimits:
      memlock:
        soft: -1
        hard: -1
      nofile:
        soft: 65536
        hard: 65536
    environment:
      - cluster.name=opensearch-cluster
      - node.name=opensearch
      - discovery.seed_hosts=opensearch
      - cluster.initial_cluster_manager_nodes=opensearch
      - bootstrap.memory_lock=true
      - plugins.security.ssl.http.enabled=false
      - "OPENSEARCH_JAVA_OPTS=-Xms512m -Xmx512m"
      - OPENSEARCH_PASSWORD=CHANGEMEPLEASE1@#a
      - OPENSEARCH_INITIAL_ADMIN_PASSWORD=CHANGEMEPLEASE1@#a
    volumes:
      - opensearch-data:/usr/share/opensearch/data
    cap_add:
      - IPC_LOCK
    ports:
      - "9200:9200"
      - "9300:9300"

volumes:
  opensearch-data:
